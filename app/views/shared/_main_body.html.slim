= render 'shared/components/navbar/left_navbar'
main.col-md-9.ml-sm-auto.col-lg-10.px-4.main-div.margin-top-navbar role="main"

	- if model == 'famille'
		.d-flex.justify-content-between.flex-wrap.flex-md-nowrap.align-items-center.pt-2.mb-1
		  h1.h2 = "Toutes les #{model}s"
		  .btn-toolbar.mb-2.mb-md-0
		    .mr-2.d-flex.justify-content-around
					= create_a_model(model)
					= link_to 'Exporter Familles', all_families_path(format: :xlsx), class: 'btn btn-outline-primary'
	  	.form-inline.pb-4.mb-5.border-bottom
		    = form_tag families_path, method: :get do
		      = text_field_tag :query,
		        params[:query],
		        class: "form-control",
		        placeholder: "Rechercher une #{model}"
		      = submit_tag :Entrer, class: 'btn btn-primary'

 	- elsif model == 'client'
	 	.d-flex.justify-content-between.flex-wrap.flex-md-nowrap.align-items-center.pt-2.mb-1
	 	  h1.h2 = "Tous les #{model}s"
	 	  .btn-toolbar.mb-2.mb-md-0
	 	    .mr-2.d-flex.justify-content-around
	 				= create_a_model(model)
	 				= link_to 'Exporter Clients', all_clients_path(format: :xlsx), class: 'btn btn-outline-primary'
	   	.form-inline.pb-4.mb-5.border-bottom
	 	    = form_tag clients_path, method: :get do
	 	      = text_field_tag :query,
	 	        params[:query],
	 	        class: "form-control",
	 	        placeholder: "Rechercher un #{model}"
	 	      = submit_tag :Entrer, class: 'btn btn-primary'

	- elsif model == 'voyage'
		.d-flex.justify-content-between.flex-wrap.flex-md-nowrap.align-items-center.pt-2.mb-1
		  h1.h2 = "Tous les #{model}s groupes"
		  .btn-toolbar.mb-2.mb-md-0
		    .mr-2.d-flex.justify-content-around
					= create_a_model(model)
					= link_to 'Exporter Groupes', all_families_path(format: :xlsx), class: 'btn btn-outline-primary'
	  	.form-inline.pb-4.mb-5.border-bottom
		    = form_tag travel_groups_path, method: :get do
		      = text_field_tag :query,
		        params[:query],
		        class: "form-control",
		        placeholder: "Rechercher un #{model}"
		      = submit_tag :Entrer, class: 'btn btn-primary'

	- elsif model == 'partenaire'
		.d-flex.justify-content-between.flex-wrap.flex-md-nowrap.align-items-center.pt-2.mb-1
		  h1.h2 = "Tous les #{model}s"
		  .btn-toolbar.mb-2.mb-md-0
		    .mr-2.d-flex.justify-content-around
					= create_a_model(model)
					= link_to ("<i class=\"fa fas fa-plus-circle\">Créer un organisme</i>").html_safe, new_partner_company_path(camp: true), class: 'btn btn-primary'
					= link_to 'Exporter Partenaires', all_partners_path(format: :xlsx), class: 'btn btn-outline-primary'
	  	.form-inline.pb-4.mb-5.border-bottom
		    = form_tag partner_companies_path, method: :get do
		      = text_field_tag :query,
		        params[:query],
		        class: "form-control",
		        placeholder: "Rechercher un #{model}"
		      = submit_tag :Entrer, class: 'btn btn-primary'


	- elsif model == 'accompagnateur'
		.d-flex.justify-content-between.flex-wrap.flex-md-nowrap.align-items-center.pt-2.mb-1
		  h1.h2 = "Tous les #{model}s"
		  .btn-toolbar.mb-2.mb-md-0
		    .mr-2.d-flex.justify-content-around
					= create_a_model(model)
					= link_to 'Exporter Accompagnateurs', all_attendants_path(format: :xlsx), class: 'btn btn-outline-primary'
	  	.form-inline.pb-4.mb-5.border-bottom
		    = form_tag attendants_path, method: :get do
		      = text_field_tag :query,
		        params[:query],
		        class: "form-control",
		        placeholder: "Rechercher un #{model}"
		      = submit_tag :Entrer, class: 'btn btn-primary'


	- elsif model == 'prospect'
		.d-flex.justify-content-between.flex-wrap.flex-md-nowrap.align-items-center.pt-2.mb-1
		  h1.h2 = "Tous les #{model}s"
		  .btn-toolbar.mb-2.mb-md-0
		    .mr-2.d-flex.justify-content-around
				  = link_to 'Exporter Prospects', all_child_details_path(format: :xlsx), class: 'btn btn-outline-primary'
	  	.form-inline.pb-4.mb-5.border-bottom
		    = form_tag child_details_path, method: :get do
		      = text_field_tag :query,
		        params[:query],
		        class: "form-control",
		        placeholder: "Rechercher un #{model}"
		      = submit_tag :Entrer, class: 'btn btn-primary'

    - elsif model == 'programme'
        .d-flex.justify-content-between.flex-wrap.flex-md-nowrap.align-items-center.pt-2.mb-1
            h1.h2 = "Tous les #{model}s"
            .btn-toolbar.mb-2.mb-md-0
                .mr-2.d-flex.justify-content-around
                = create_a_model(model)
                = link_to 'Exporter Programmes', all_child_details_path(format: :xlsx), class: 'btn btn-outline-primary'
        .form-inline.pb-4.mb-5.border-bottom
            = form_tag programs_path, method: :get do
                = text_field_tag :query,
            params[:query],
            class: "form-control",
            placeholder: "Rechercher un #{model}"
            = submit_tag :Entrer, class: 'btn btn-primary'


	.d-flex.justify-content-center
		= render 'shared/components/table'
