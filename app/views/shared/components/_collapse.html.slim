tr class='collapse' id='collapse#{family.id}'data-parent="#accordion" aria-labelledby="heading#{family.id}"
	td colspan="6"
		.card
			.card-body.d-flex.justify-content-around
				p oliiviiiier
			  	= link_to ('<p>Peter<i class="fa fas fa-edit></i></p>').html_safe, edit_family_path(family), class: 'btn btn-primary text-center'
			  	p dzdz
			  	= link_to ('<i class="fa far fa-trash-alt></i>').html_safe, family_path(family), method: :delete, class: 'btn btn-sm btn-primary', data: {confirm: "Êtes-vous sûr?"}
  	/ = link_to 'Plus de détails', family_path(family), class: 'btn btn-primary'