nav.col-md-2.d-none.d-md-block.bg-light.sidebar
  .sidebar-sticky
    div.nav.flex-column
      = link_to ('<i class="fa fas fa-users icon d-flex justify-content-between">Familles</i>').html_safe, families_path, class: 'item', remote: true
      = link_to ('<i class="fa far fa-user-circle icon d-flex justify-content-between">Clients</i>').html_safe, clients_path, class: 'item', remote: true
      = link_to ('<i class="fa fas fa-plane icon d-flex justify-content-between">Voyages</i>').html_safe, travels_path, class: 'item', remote: true
      = link_to ('<i class="fa fas fa-handshake icon d-flex justify-content-between">Partenaires</i>').html_safe, partner_companies_path, class: 'item', remote: true
      = link_to ('<i class="fa fas fa-handshake icon d-flex justify-content-between">Accompagnateurs</i>').html_safe, attendants_path, class: 'item', remote: true